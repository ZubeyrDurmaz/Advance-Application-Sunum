spring.application.name=backend

# ── Database ──
spring.datasource.url=jdbc:postgresql://35.234.82.133:5432/watch_store_db
spring.datasource.username=postgres
spring.datasource.password=MarcoAsensioWillemsen21
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database (Local Testing) - Commented out
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.driver-class-name=org.h2.Driver
# spring.h2.console.enabled=true

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── JWT ──
app.jwt.secret=ChronosSecretKeyForJWTTokenGeneration2024MustBeLongEnoughForHS512Algorithm!!
app.jwt.access-token-expiration=900000
app.jwt.refresh-token-expiration=604800000

# ── Server ──
server.port=8080
